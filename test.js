app.controller('testCtrl',function($scope,TimeFnService,globalService){
	// $scope.DisplayDate = TimeFnService.incDate(new Date(),0,1,0,0);
	// $scope.nowDate = new Date();
	// $scope.testglobal = globalService.freezehrs;

});